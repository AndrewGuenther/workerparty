{
  "name": "workerparty",
  "version": "0.0.0",
  "description": "An implementation of MapReduce which uses WebWorkers in the browser to run tasks.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrew Guenther",
  "license": "Apache v2.0",
  "dependencies": {
    "express": "^4.12.2",
    "socket.io": "^1.3.5",
    "http": "0.0.0"
  },
  "devDependencies": {
    "grunt": "^0.4.5",
    "coffee-script": "^1.9.1",
    "browserify": "^9.0.3",
    "grunt-contrib-coffee": "^0.13.0",
    "grunt-browserify": "^3.5.0",
    "grunt-contrib-clean": "^0.6.0"
  }
}
